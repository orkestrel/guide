// Dummy fixture test file — linked by the good guide's `## Tests` section but
// never executed: `tests/fixtures/**` is outside every vitest project's
// include glob (`tests/src/**` / `tests/guides/**`).
export {}
