# Guides (broken: empty-manifest)

> A `## By concept` manifest whose table carries zero rows — `parseManifest`
> returns `[]`, so a drop-in wired to this manifest must fail its own
> `manifest lists at least one guide` non-vacuousness guard rather than
> passing an empty suite vacuously.

## By concept

| Concept | Spec | Source | Tests |
| ------- | ---- | ------ | ----- |
