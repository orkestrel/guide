# Missing Example

> Fixture guide (BROKEN: missing-example) — `farewell` has no fence mention and
> no `@example` JSDoc; EX's `findUnexampled` catches it with `['farewell']`.

## Surface

### Helpers

| Name       | Kind     |
| ---------- | -------- |
| `greet`    | function |
| `farewell` | function |

## Patterns

```ts
import { greet } from '../module/helpers.js'

greet('world')
```
